# express-js-microservice-template
express js + JWT Auth + firebase custom auth + docker + postgres sql



```Bash
npm install bcryptjs cookie-parser cors dotenv express firebase-admin jsonwebtoken morgan path pg
```
